import { RemoteCreateMachine } from './remoteCreateMachine'
import { RemoteDeleteMachine } from './remoteDeleteMachine'
import { RemoteGetMachine } from './remoteGetMachine'
import { RemoteGetMachines } from './remoteGetMachines'
import { RemoteUpdateMachine } from './remoteUpdateMachine'

export const MachineData = {
  RemoteCreateMachine,
  RemoteDeleteMachine,
  RemoteGetMachine,
  RemoteGetMachines,
  RemoteUpdateMachine,
}
